<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar">

        <!-- Launcher Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".StartPageActivity" android:exported="true" />
        <activity android:name=".HomeActivity" android:exported="true" />
        <activity android:name=".HouseSpecificationActivity" android:exported="true" />
        <activity android:name=".BasedOnReviewActivity" android:exported="true" />
        <activity android:name=".ConstructionMaterialsActivity" android:exported="true" />
        <activity android:name=".ContractorSelectionActivity" android:exported="true" />
        <activity android:name=".ProfileBasedContractorsActivity" android:exported="true" />
        <activity android:name=".ReviewBasedContractorsActivity" android:exported="true" />
        <activity android:name=".ImageAdapter" android:exported="true" />
        <activity android:name=".MainActivity" android:exported="true" />

    </application>

</manifest>
